<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="csci4620.blueprint" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".LoginActivity"
            android:label="@string/login_label" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".RegistrationActivity"
            android:label="@string/register_label" >
        </activity>

        <activity
            android:name=".AddRoomActivity"
            android:label="@string/add_room_label" >
        </activity>

        <activity
            android:name=".AddFurnitureActivity"
            android:label="@string/add_furniture_label" >
        </activity>

        <activity
            android:name=".DimensionsActivity"
            android:label="@string/dimensions_label" >
        </activity>

    </application>

</manifest>
